## Instructor-Backend

*assuming `docker` and `docker-compose` already installed*


#### how to activate service

```bash
docker-compose up --build
# use `ctrl+z` or `ctrl+c` to close the containers

```

#### how to properly close services

```bash
docker-compose down -v
```